# SORI App icons

These bundle icons are generated from `icon.png` only.

Do not run automatic icon regeneration or replace `icon.ico` / `icon.icns`
without explicit owner approval. Keep `icon.png` as the single source image
and rebuild platform icons from that source when approved.
