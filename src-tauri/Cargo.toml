[package]
name = "sori-app"
version = "0.1.0"
description = "SORI desktop client"
authors = ["SORI"]
edition = "2021"

[lib]
name = "sori_app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
